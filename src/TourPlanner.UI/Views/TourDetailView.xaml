<UserControl x:Class="TourPlanner.UI.Views.TourDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:TourDetailViewModel}"
             xmlns:local="clr-namespace:TourPlanner.UI.ViewModels">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Name, TargetNullValue=No tour selected}"
                   FontSize="18" FontWeight="Semibold" />

        <!-- Details -->
        <GroupBox Grid.Row="2" Header="Details" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Name:"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Description:"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="From:"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding From, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Text="To:"/>
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding To, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Text="Transport:"/>
                <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding TransportType, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="10" Grid.Column="0" VerticalAlignment="Center" Text="Distance (km):"/>
                <TextBlock Grid.Row="10" Grid.Column="1"
                           Text="{Binding DistanceKm, StringFormat={}{0:F1}}" />

                <TextBlock Grid.Row="12" Grid.Column="0" VerticalAlignment="Center" Text="Estimated Time:"/>
                <TextBlock Grid.Row="12" Grid.Column="1"
                           Text="{Binding EstimatedTime, StringFormat={}{0:hh\\:mm\\:ss}}" />
            </Grid>
        </GroupBox>

        <!-- Logs -->
        <GroupBox Grid.Row="4" Header="Logs" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0"
                          ItemsSource="{Binding Logs}"
                          SelectedItem="{Binding SelectedLog}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Width="*" />
                        <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}" Width="*" />
                        <DataGridTextColumn Header="Distance" Binding="{Binding TotalDistance, StringFormat={}{0:F1}}" Width="*" />
                        <DataGridTextColumn Header="Time" Binding="{Binding TotalTime, StringFormat={}{0:hh\\:mm\\:ss}}" Width="*" />
                        <DataGridTextColumn Header="Rating" Binding="{Binding Rating}" Width="*" />
                        <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>

                <Grid Grid.Row="2" Margin="0,0,0,4">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="2*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <!-- Headers -->
                    <TextBlock Grid.Row="0" Grid.Column="0" Text="Date" Margin="0,0,4,2" />
                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Difficulty" Margin="0,0,4,2" />
                    <TextBlock Grid.Row="0" Grid.Column="2" Text="Distance" Margin="0,0,4,2" />
                    <TextBlock Grid.Row="0" Grid.Column="3" Text="Time" Margin="0,0,4,2" />
                    <TextBlock Grid.Row="0" Grid.Column="4" Text="Rating" Margin="0,0,4,2" />
                    <TextBlock Grid.Row="0" Grid.Column="5" Text="Comment" Margin="0,0,0,2" />

                    <!-- Inputs -->
                    <DatePicker Grid.Row="1" Grid.Column="0" SelectedDate="{Binding LogDate, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Grid.Row="1" Grid.Column="1" Width="40" Text="{Binding LogDifficulty, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Grid.Row="1" Grid.Column="2" Width="60" Text="{Binding LogDistance, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Grid.Row="1" Grid.Column="3" Width="80" Text="{Binding LogTime, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Grid.Row="1" Grid.Column="4" Width="40" Text="{Binding LogRating, UpdateSourceTrigger=PropertyChanged}" />
                    <TextBox Grid.Row="1" Grid.Column="5" Text="{Binding LogComment, UpdateSourceTrigger=PropertyChanged}" />
                </Grid>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,4,0,0">
                    <Button Content="Add Log" Command="{Binding AddLogCommand}" Margin="0,0,8,0"/>
                    <Button Content="Delete Log" Command="{Binding DeleteLogCommand}" />
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
