<UserControl x:Class="TourPlanner.UI.Views.TourDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=local:TourDetailViewModel}"
             xmlns:local="clr-namespace:TourPlanner.UI.ViewModels">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="8"/>
            <RowDefinition Height="2*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="{Binding Name, TargetNullValue=No tour selected}"
                   FontSize="18" FontWeight="Semibold" />

        <!-- Details -->
        <GroupBox Grid.Row="2" Header="Details" Padding="8">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="160"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" VerticalAlignment="Center" Text="Name:"/>
                <TextBox Grid.Row="0" Grid.Column="1"
                         Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Text="Description:"/>
                <TextBox Grid.Row="2" Grid.Column="1"
                         Text="{Binding Description, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Text="From:"/>
                <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding From, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="6" Grid.Column="0" VerticalAlignment="Center" Text="To:"/>
                <TextBox Grid.Row="6" Grid.Column="1" Text="{Binding To, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="8" Grid.Column="0" VerticalAlignment="Center" Text="Transport:"/>
                <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding TransportType, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Grid.Row="10" Grid.Column="0" VerticalAlignment="Center" Text="Distance (km):"/>
                <TextBlock Grid.Row="10" Grid.Column="1"
                           Text="{Binding DistanceKm, StringFormat=F1}" />

                <TextBlock Grid.Row="12" Grid.Column="0" VerticalAlignment="Center" Text="Estimated Time:"/>
                <TextBlock Grid.Row="12" Grid.Column="1"
                           Text="{Binding EstimatedTime}" />
            </Grid>
        </GroupBox>

        <!-- Logs -->
        <GroupBox Grid.Row="4" Header="Logs" Padding="8">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="8"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <DataGrid Grid.Row="0"
                          ItemsSource="{Binding Logs}"
                          SelectedItem="{Binding SelectedLog}"
                          AutoGenerateColumns="False"
                          CanUserAddRows="False"
                          IsReadOnly="True">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Date" Binding="{Binding Date, StringFormat=\{0:yyyy-MM-dd\}}" Width="*" />
                        <DataGridTextColumn Header="Difficulty" Binding="{Binding Difficulty}" Width="*" />
                        <DataGridTextColumn Header="Distance" Binding="{Binding TotalDistance}" Width="*" />
                        <DataGridTextColumn Header="Time" Binding="{Binding TotalTime}" Width="*" />
                        <DataGridTextColumn Header="Rating" Binding="{Binding Rating}" Width="*" />
                        <DataGridTextColumn Header="Comment" Binding="{Binding Comment}" Width="2*" />
                    </DataGrid.Columns>
                </DataGrid>

                <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,0">
                    <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                        <TextBlock Text="Date"/>
                        <DatePicker SelectedDate="{Binding LogDate, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                        <TextBlock Text="Difficulty"/>
                        <TextBox Width="40" Text="{Binding LogDifficulty, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                        <TextBlock Text="Distance"/>
                        <TextBox Width="60" Text="{Binding LogDistance, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                        <TextBlock Text="Time"/>
                        <TextBox Width="80" Text="{Binding LogTime, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Margin="0,0,8,0">
                        <TextBlock Text="Rating"/>
                        <TextBox Width="40" Text="{Binding LogRating, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Orientation="Vertical" Width="200">
                        <TextBlock Text="Comment"/>
                        <TextBox Text="{Binding LogComment, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                </StackPanel>

                <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Content="Add Log" Command="{Binding AddLogCommand}" Margin="0,0,8,0"/>
                    <Button Content="Delete Log" Command="{Binding DeleteLogCommand}" />
                </StackPanel>
            </Grid>
        </GroupBox>
    </Grid>
</UserControl>
